<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Message board</title>
</head>
<body>
  <h1><%= title %></h1>
  <a href="/new" class="new-message">New message</a>
  <ul>
    <% messages.forEach((message, index) => { %>
      <li>
        <p><%= message.text %></p>
        <p><%= message.user %></p>
        <a href="/details/<%= index %>" class="open">Open</a>
      </li>
    <% }); %>
  </ul>
</body>
</html>